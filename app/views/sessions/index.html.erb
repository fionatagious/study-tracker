<p id="notice"><%= notice %></p>

<h1>Stopwatch</h1>
  <td>
    <%= button_to 'start', {action: :new}, method: :get %>
    <%= button_to 'stop', {action: :new}, method: :get  %>
    <% if params.key?(:start) %>
      <p class="leading-relaxed"><span>[works] Start time is <%= params[:start] %></span></p>
    <% else %>
      <p class="leading-relaxed"><span>Stop time is <%= params[:stop].to_i.to_s %></span></p>
      <%#= link_to 'Create Session', new_session_path  %>
  <% end %>
  </td>
<!--  <td>-->
<!--    <p> Start is <%#= Time.at(params[:start].to_i).to_s %></p>-->
<!--    <p> Stop is <%#= Time.at(params[:stop].to_i).to_s %></p>-->
<!--  </td>-->


  


<h1>Sessions</h1>

<table>
  <thead>
    <tr>
      <th>Language</th>
      <th>Project</th>
      <th>Buddies</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sessions.each do |session| %>
      <tr>
        <td><%= session.language %></td>
        <td><%= session.project %></td>

        <td><%= link_to 'Show', session %></td>
        <td><%= link_to 'Edit', edit_session_path(session) %></td>
        <td><%= link_to 'Destroy', session, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Session', new_session_path %>
